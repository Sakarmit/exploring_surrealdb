# Core SurrealDB driver
surrealdb>=0.3.0

# PDF parsing (pick one; all are optional — parsers auto-detect)
PyMuPDF>=1.23.0        # best quality, recommended
# pdfplumber>=0.10.0   # alternative
# pypdf>=3.0.0         # lightweight alternative

# Image processing (optional — fallback reads PNG/JPEG dimensions without it)
Pillow>=10.0.0

# HTTP requests (for --http REST API mode)
requests>=2.31.0

# Jupyter notebooks (optional, for demo_notebook.py)
# jupytext>=1.16.0     # converts .py notebook to .ipynb
# jupyter>=1.0.0
